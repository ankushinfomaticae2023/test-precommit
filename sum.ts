// Define a function to calculate the sum of two numbers
const sum = (c: number, d: number): number => {
    // Calculate the sum of the two input numbers
    const result = c + d;
    // Return the result
    return result;
}

// Call the sum function with the numbers 2 and 4 and log the result to the console
console.log(sum(2,4))
