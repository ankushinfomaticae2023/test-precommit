// Function to perform subtraction operation
const sub = (c: number, d: number): number => {
  // Calculate the result
  const result = c - d;
  
  // Return the result
  return result;
}

// Test the function
console.log(sub(2,4))
