// Function for subtracting two numbers
const sub = (c: number, d: number): number => {
  // Subtracting the two input numbers
  const result = c - d;
  // Returning the result
  return result;
}

// Logging the result of the subtraction function
console.log(sub(2,4))
