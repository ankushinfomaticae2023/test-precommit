// Function to calculate the difference of two numbers
const sum = (c: number, d: number): number => {
  // Subtract second number from the first
  const result = c - d;
  // Return the result
  return result;
}

// Log the result of the function with inputs 2 and 4
console.log(sum(2,4))
